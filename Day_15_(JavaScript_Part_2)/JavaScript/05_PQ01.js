let color = "green";

if (color === "red") { // if the condition is true then the code inside the block will
  console.log("Stop!");
}

if (color === "yellow") { // if the condition is true then the code inside the block will
  console.log("Slow down!");
}

if (color === "green") { // if the condition is true then the code inside the block will
  console.log("Go");
}