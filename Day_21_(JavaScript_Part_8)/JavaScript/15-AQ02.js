// Q: Create a new array using the map() method whose each element is equal to the original element plus 5.

const num = [2, 4, 10];
console.log(num);

let numPlus5 = num.map( (el) => el + 5);
console.log(numPlus5);
