let heroes = [["superman", "batman", "wonder woman"], ["spiderman", "ironman", "thor"]];

// Iterate over the 2D array using a for-of loop
for (list of heroes) {
  for (hero of list) {
    console.log(hero);
  }
}
