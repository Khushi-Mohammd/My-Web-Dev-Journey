// num = Math.random(); // It will generate a random floating-point number between 0 and 1.
// console.log(num);

// num *= 10; // Now, the value of num will be from 0 to 9.
// console.log(num);

// num = Math.floor(num); // Now, the fractional part of the number will be removed.
// console.log(num);

// num +=1; // Now, the value of num will be from 1 and 10.
// console.log(num);

// We can combine all the above steps in a single line.
console.log(Math.floor(Math.random() * 10) + 1);