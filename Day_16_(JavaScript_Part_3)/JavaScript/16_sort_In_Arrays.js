let colors = ["Red", "Yellow", "Blue", "Orange", "Pink", "White"];
console.log(colors);

colors.sort(); // Sorts the array in ascending order.
console.log(colors);

let chars = ["b", "d", "e", "a"];
console.log(chars);

chars.sort(); // Sorts the array in ascending order.
console.log(chars);

let marks = [99, 89, 67, 42, 100];
console.log(marks);

marks.sort(); // The sort() method sorts the array elements as strings.
console.log(marks);