// let msg = "          Hello          ";

// console.log(msg.trim(););
// String 'msg' is still same as before because Strings are Immutable in JS.


let password = prompt("Set your password : ");
let newPassword = password.trim();
console.log(newPassword);
