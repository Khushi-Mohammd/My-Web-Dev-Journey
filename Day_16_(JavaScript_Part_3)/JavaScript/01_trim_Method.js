// let msg = "          Hello          ";

// console.log(msg.trim(););
// String 'msg' is still same as before because Strings are Immutable in JS.


let password = prompt("Set your password : "); // It will take the input from the user.
let newPassword = password.trim(); // It will remove the extra spaces from the string.
console.log(newPassword); // It will print the new password without extra spaces.
