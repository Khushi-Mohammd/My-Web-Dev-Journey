let cars = ["Audi", "BMW", "Aston Martin", "Lemborgini", "Rolls Royce"];
console.log(cars);

cars.push("Ferrari"); // Adds a new element at the end of the array.
console.log(cars);

cars.push("Range Rover"); // Adds a new element at the end of the array.
console.log(cars);

cars.pop(); // Removes the last element from the array.
console.log(cars);

cars.pop(); // Removes the last element from the array.
console.log(cars);

cars.unshift("Ferrari"); // Adds a new element at the beginning of the array.
console.log(cars);

cars.unshift("Range Rover"); // Adds a new element at the beginning of the array.
console.log(cars);

cars.shift(); // Removes the first element from the array.
console.log(cars);

cars.shift(); // Removes the first element from the array.
console.log(cars);