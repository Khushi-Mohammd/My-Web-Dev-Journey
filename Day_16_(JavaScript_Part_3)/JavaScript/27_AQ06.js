let colors = ["Red", "Yellow", "Blue", "Orange", "Pink", "White"];

let find = "Yellow";

if (colors.indexOf(find) != -1) {
  console.log(`Yes array "colors" contains "${find}"`);
} else {
  console.log(`No array "colors" does not contains "${find}"`);
}